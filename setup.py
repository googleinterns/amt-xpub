"""Installs bloom_filter_buffling_blipping_evaluation."""
from setuptools import setup, find_packages

setup(
    name='bloom_filter_buffling_blipping_evaluation',
    version='0.1.0',
    description='Evaluation of buffling+blipping for bloom filter',
    author='Fan Chen',
    url='https://github.com/googleinterns/amt-xpub.git',
    license=license,
    packages=find_packages(exclude=('tests', 'docs'))
)